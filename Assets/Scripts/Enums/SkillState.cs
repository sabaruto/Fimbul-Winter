namespace Enums
{
  /// <summary>
  ///   This enum holds the different states the player's skills can
  ///   be in. This ensures that stupid mistakes aren't made
  /// </summary>
  public enum SkillState { Default, Left, Right }
}